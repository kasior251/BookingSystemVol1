@model BookingSystem.Models.Ticket

@{
    ViewBag.Title = "showConfirmation";
}

<h2>showConfirmation</h2>

<div>
    <h4>Ticket</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>
<p>
    Id nr: @Model.id;
    @for (int i = 0; i < Model.passengers.Count; i++)
    {
        @Model.passengers[i].firstName;
        @Model.passengers[i].lastName;
        @Model.passengers[i].birthDate;


    }
    
</p>
